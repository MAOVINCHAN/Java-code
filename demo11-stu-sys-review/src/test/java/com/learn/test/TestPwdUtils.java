package com.learn.test;

import com.learn.Utils.PwdUtils;

import java.lang.reflect.Method;
import java.util.Map;
import java.util.Set;

public class TestPwdUtils {
    public static void main(String[] args) {
        // String hash = PwdUtils.hashPassword("123456");
        // System.out.println("hash = " + hash);
        // boolean b = PwdUtils.checkPassword("123456", hash);
        // System.out.println("b = " + b);

        // 获取所有系统环境变量
        // Map<String, String> env = System.getenv();
        // Set<Map.Entry<String, String>> entries = env.entrySet();
        // entries.forEach( item -> {
        //     String key = item.getKey();
        //     String value = item.getValue();
        //     System.out.println(key + "=" + value);
        // });

        // 打印对象身上的所有方法
        // Class<System> systemClass = System.class;
        // Method[] methods = systemClass.getMethods();
        // for (int i = methods.length - 1; i >= 0; i--) {
        //     Method method = methods[i];
        //     String name = method.getName();
        //     System.out.println("method[" + i + "]=" + name);
        // }
    }
}
